package com.seacroak.basicweapons.item;

import net.minecraft.item.ToolMaterial;

public class BasicWeaponSweeplessItem extends BasicWeaponItem {
  public BasicWeaponSweeplessItem(ToolMaterial toolMaterial, int attackDamage, float attackSpeed, Settings settings) {
    super(toolMaterial, attackDamage, attackSpeed, settings);
  }
}
